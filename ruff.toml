extend-exclude = ["venv", "websock/venv", "utils/certs", "scripts/cnet"]

# Lints under ruff's own defaults for the version CI pins
# (.github/workflows/ci.yml). That pin is what keeps the result stable: ruff's
# default rule set moves between minors (0.16 grew it from 59 rules to 413), so
# bump it deliberately and fix what the new version flags in the same change.
